# We use a white-list approach here, so ignore everything and then make exceptions.

*

!src/*
!public/*
!server/*

!package.json
!yarn.lock
!tsconfig.json

!webpack.config.ts
!.babelrc
!.env

!Dockerfile